# P1-03 内容发布任务

- 状态固定为：`selecting_images`（待选图）、`writing_copy`（待写文案）、`ready_to_publish`（待发布）、`published`（已发布）。
- 完成服务时按订单唯一创建内容发布任务，并从行动中心跳转到对应任务。
- 文案模板仅提供结构化字段：标题、服务摘要、设计亮点、预约引导和标签；不接入 AI，不自动生成或改写内容。
- 已发布必须记录发布平台、发布时间和原始内容链接；待发布至少需要标题。
- 同一订单只有一个内容发布任务，重复完成或重试不会重复创建。
