# P0-20 从订单创建作品

- “创建作品案例”行动任务直接进入带 `orderId` 的作品编辑页。
- 后端仅允许从当前美甲师的已完成订单创建作品；同一订单只创建一个草稿。
- 草稿自动带入客户、预约、正式服务、实收金额和服务照片，并保留订单上的渠道归因链路。
- 自动创建的作品始终为内部草稿、默认不可见、授权状态待确认，可继续选图和编辑。
- 发布前校验标题、封面、客户照片授权和客户专属访问关系；没有统一有效授权时禁止发布。
- 公开作品 DTO 不输出 `sourceOrderId`、客户或预约关联；这些字段只在经营端详情中使用。
