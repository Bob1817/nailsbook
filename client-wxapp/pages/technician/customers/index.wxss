page {
  background: var(--bg-gradient-entry);
  height: 100vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.page {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* ===== 固定头部 ===== */
.header {
  flex-shrink: 0;
  background: var(--bg-card);
  padding: 16rpx 32rpx 12rpx;
  border-bottom: 1rpx solid #f1ecef;
}

.search-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

/* 搜索框 */
.search-bar {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-height: 88rpx;
  padding: 0 26rpx;
  background: var(--bg-input);
  border: 2rpx solid transparent;
  border-radius: var(--radius-lg);
  min-width: 0;
}

.btn-invite {
  display: flex;
  align-items: center;
  gap: 8rpx;
  min-height: 88rpx;
  padding: 0 22rpx;
  background: var(--bg-card);
  border: 2rpx solid var(--border-light);
  border-radius: var(--radius-lg);
  flex-shrink: 0;
}

.btn-invite:active {
  background: var(--bg-tag);
}

.invite-icon {
  font-size: var(--font-md);
  color: var(--brand-primary);
  font-weight: var(--weight-semibold);
}

.invite-text {
  font-size: var(--font-sm);
  color: var(--brand-primary);
  font-weight: var(--weight-semibold);
}

.search-icon {
  width: 24rpx; height: 24rpx; border: 3rpx solid var(--text-muted); border-radius: 50%; position: relative;
  flex-shrink: 0;
}
.search-icon::after { content: ''; position: absolute; width: 10rpx; height: 3rpx; right: -8rpx; bottom: -4rpx; background: var(--text-muted); transform: rotate(45deg); border-radius: 3rpx; }

.search-input {
  flex: 1;
  font-size: var(--font-sm);
  color: var(--text-primary);
}

.search-clear {
  width: 88rpx;
  height: 88rpx;
  margin-right: -26rpx;
  border-radius: 50%;
  background: transparent;
  color: var(--text-muted);
  font-size: var(--font-xs);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.clear-line { width: 24rpx; height: 3rpx; background: #6b7280; transform: rotate(45deg); position: relative; }.clear-line::after { content: ''; position: absolute; inset: 0; background: #6b7280; transform: rotate(90deg); }

/* 轻量筛选工具栏 */
.filter-row { min-height:88rpx; display:flex; align-items:center; gap:12rpx; }
.filter-row-stage { gap:0; }
.tab-scroll { flex:1; min-width:0; white-space:nowrap; }
.filter-label { width:58rpx; flex-shrink:0; color:#9a8f96; font-size: var(--font-xs); font-weight: var(--weight-semibold); }

.tab-list {
  display: inline-flex;
  gap: 8rpx;
}

.tab-item {
  display: inline-flex;
  align-items: center;
  height: 76rpx;
  min-height: 76rpx;
  padding: 0 24rpx;
  border-radius: 999rpx;
  background: transparent;
  border: 2rpx solid transparent;
  font-size: var(--font-sm);
  color: var(--text-muted);
  flex-shrink: 0;
  font-weight: var(--weight-medium);
}

.tab-item:active {
  background: var(--bg-tag);
}

.tab-active {
  background: var(--bg-tag);
  border-color: transparent;
  color: var(--brand-primary);
  font-weight: var(--weight-bold);
}

/* ===== 可滚动列表区 ===== */
.list-scroll {
  flex: 1;
  min-height: 0;
}

.list-area {
  padding: var(--space-md) 40rpx calc(24rpx + env(safe-area-inset-bottom));
}

/* 骨架屏 */
.skeleton-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.skel-card {
  height: 280rpx;
  border-radius: var(--radius-lg);
  background: linear-gradient(90deg, #f5f0f3, #faf2f5, #f5f0f3);
  background-size: 200% 100%;
  animation: skel 1.4s linear infinite;
}

@keyframes skel {
  0% { background-position: 0 0; }
  100% { background-position: -200% 0; }
}

/* 空状态 */
.empty-card {
  margin: 80rpx 0;
  padding: 80rpx 40rpx;
  background: var(--bg-card);
  border-radius: var(--radius-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.empty-icon {
  width: 112rpx; height: 112rpx; border-radius: 50%; background: var(--bg-tag); display: flex; align-items: center; justify-content: center;
}
.empty-avatar-mark { width: 42rpx; height: 42rpx; border-radius: 50%; background: #e786aa; position: relative; }.empty-avatar-mark::after { content: ''; position: absolute; left: -14rpx; right: -14rpx; top: 48rpx; height: 28rpx; border-radius: var(--radius-lg) 28rpx 10rpx 10rpx; background: #e786aa; }
.empty-reset { display: flex; min-height: 88rpx; margin-top: 20rpx; padding: 0 38rpx; align-items: center; justify-content: center; border-radius: var(--radius-xl); background: #fff1f5; color: #a64f73; font-size: var(--font-sm); font-weight: var(--weight-semibold); }

.empty-title {
  font-size: var(--font-md);
  font-weight: var(--weight-semibold);
  color: var(--text-primary);
}

.empty-sub {
  font-size: var(--font-sm);
  color: var(--text-muted);
  text-align: center;
}

/* ===== 客户卡 ===== */
.customer-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.customer-card {
  padding: 24rpx 24rpx 20rpx;
  overflow: hidden;
  background: var(--bg-card);
  border: 2rpx solid var(--border-light);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  transition: transform 120ms ease, box-shadow 120ms ease;
}

.customer-card:active {
  transform: scale(0.995);
}

.customer-card-header {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

/* 头像 */
.avatar {
  flex-shrink: 0;
  width: 104rpx;
  height: 104rpx;
  border-radius: 50%;
  background: #fdecef;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.avatar-img {
  width: 100%;
  height: 100%;
}

.avatar-char {
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
  color: #e86b8f;
}

.identity-area {
  flex: 1;
  min-width: 0;
}

.identity-title-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  min-width: 0;
  height: 48rpx;
}

.customer-name {
  flex: 0 1 auto;
  min-width: 0;
  font-size: var(--font-lg);
  font-weight: var(--weight-semibold);
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.lifecycle-badge { flex-shrink:0; display:flex; height:40rpx; padding:0 14rpx; align-items:center; justify-content:center; box-sizing:border-box; border-radius:999rpx; font-size: var(--font-xs); font-weight: var(--weight-semibold); line-height:1; white-space:nowrap; }
.lifecycle-new { background: var(--tone-blue-bg); color: var(--tone-blue-text); }
.lifecycle-active { background: var(--tone-emerald-bg); color: var(--tone-emerald-text); }
.lifecycle-due { background: var(--tone-amber-bg); color: var(--tone-amber-text); }
.lifecycle-dormant { background: var(--tone-gray-bg); color: var(--tone-gray-text); }

.card-entry { flex-shrink:0; display:flex; width:88rpx; height:88rpx; margin-right:-20rpx; align-items:center; justify-content:center; border-radius:50%; }
.card-entry:active { background:#faf5f7; }
.card-chevron {
  width: 14rpx;
  height: 14rpx;
  border-top: 3rpx solid #aaa0a6;
  border-right: 3rpx solid #aaa0a6;
  transform: rotate(45deg);
}

.address-row { display:flex; align-items:center; gap:10rpx; margin-top:10rpx; }
.location-icon { flex-shrink:0; width:14rpx; height:14rpx; border:3rpx solid #aaa0a6; border-radius:50% 50% 50% 0; transform:rotate(-45deg); }
.customer-addr {
  min-width: 0;
  font-size: var(--font-sm);
  color: var(--text-muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.customer-status { display:flex; min-height:88rpx; margin-top:18rpx; padding:12rpx 18rpx; align-items:center; gap:12rpx; border-radius:var(--radius-md); box-sizing:border-box; font-size: var(--font-xs); }
.status-dot { flex-shrink:0; width:12rpx; height:12rpx; border-radius:50%; }
.status-copy { flex:1; min-width:0; color:var(--text-body); }
.status-date { flex-shrink:0; color:var(--text-muted); }
.lifecycle-new-surface { background: var(--tone-blue-bg); }
.lifecycle-active-surface { background: var(--tone-emerald-bg); }
.lifecycle-due-surface { background: var(--tone-amber-bg); }
.lifecycle-dormant-surface { background: var(--tone-gray-bg); }
.lifecycle-new-dot { background: var(--tone-blue-text); }
.lifecycle-active-dot { background: var(--tone-emerald-text); }
.lifecycle-due-dot { background: var(--tone-amber-text); }
.lifecycle-dormant-dot { background: var(--tone-gray-text); }

/* 标签 */
.tags-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 18rpx;
}

.tag-pill {
  padding: 6rpx 20rpx;
  border-radius: var(--radius-md);
  font-size: var(--font-xs);
  font-weight: var(--weight-medium);
}

/* 统计网格 */
.stats-grid {
  display: flex;
  margin-top: 20rpx;
  padding: 22rpx 0;
  border-top: 2rpx solid #f2edf0;
  border-bottom: 2rpx solid #f2edf0;
}

.stat-item {
  flex: 1;
  min-width: 0;
  padding: 0 20rpx;
  border-left: 2rpx solid #f2edf0;
}
.stat-item:first-child { padding-left:0; border-left:0; }
.stat-item:last-child { padding-right:0; }

.stat-label {
  display: block;
  font-size: var(--font-xs);
  color: var(--text-muted);
  margin-bottom: 6rpx;
}

.stat-value {
  display: block;
  font-size: var(--font-sm);
  font-weight: var(--weight-medium);
  color: var(--text-body);
}

.stat-price {
  color: var(--brand-primary);
  font-size: var(--font-md);
  font-weight: var(--weight-bold);
}
