<nav-bar title="{{title}}" showBack/><view class="page"><view wx:if="{{items.length}}" class="list"><view wx:for="{{items}}" wx:key="index" class="row" bindtap="open" data-index="{{index}}"><view><text class="name">{{item.name || item.nickname || item.orderNo || item.lostReason || item.cancelReason || item.noShowReason || '未填写原因'}}</text><text class="meta">{{item.sourceChannel || item.status || item.lifecycleStage || ''}}</text></view><text class="arrow">›</text></view></view><view wx:else class="empty">当前筛选范围暂无明细</view></view>
