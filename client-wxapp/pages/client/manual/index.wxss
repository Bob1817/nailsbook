page {
  background: var(--bg-page);
  height: 100vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.page {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.scroll { flex: 1; min-height: 0; }

.content {
  padding: var(--space-md) 32rpx calc(48rpx + env(safe-area-inset-bottom));
}

/* 区块标题 */
.block-head {
  padding: 16rpx 8rpx;
  margin-top: 8rpx;
}
.block-title {
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
  color: var(--text-primary);
  margin-right: 16rpx;
}
.block-sub {
  font-size: var(--font-sm);
  color: var(--text-muted);
}

/* 使用指引 */
.guides {
  background: var(--bg-card);
  border-radius: var(--radius-md);
  padding: 12rpx 28rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.05);
}
.guide {
  display: flex;
  gap: 20rpx;
  padding: var(--space-md) 0;
  border-bottom: 0;
}
.guide:last-child { border-bottom: none; }
.guide-num {
  flex-shrink: 0;
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  background: var(--brand-gradient);
  color: var(--text-inverse);
  font-size: var(--font-sm);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
  justify-content: center;
}
.guide-body { flex: 1; min-width: 0; }
.guide-t {
  display: block;
  font-size: var(--font-md);
  font-weight: var(--weight-semibold);
  color: var(--text-primary);
  margin-bottom: 8rpx;
}
.guide-d {
  display: block;
  font-size: var(--font-sm);
  color: var(--text-muted);
  line-height: 1.7;
}

/* 常见问题 */
.faqs {
  background: var(--bg-card);
  border-radius: var(--radius-md);
  padding: 4rpx 28rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.05);
}
.faq {
  padding: 28rpx 0;
  border-bottom: 0;
}
.faq:last-child { border-bottom: none; }
.faq-q-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}
.faq-q {
  flex: 1;
  font-size: var(--font-md);
  font-weight: var(--weight-medium);
  color: var(--text-primary);
}
.faq-chevron {
  flex-shrink: 0;
  font-size: var(--font-lg);
  color: var(--text-muted);
  transform: rotate(90deg);
  transition: transform 0.2s;
}
.faq-open .faq-chevron { transform: rotate(-90deg); }
.faq-a-wrap {
  margin-top: var(--space-sm);
  padding: 20rpx 24rpx;
  background: var(--bg-input);
  border-radius: var(--radius-sm);
}
.faq-a {
  font-size: var(--font-sm);
  color: var(--text-body);
  line-height: 1.75;
}

/* 仍有疑问 */
.more {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
  padding: 56rpx 0 16rpx;
}
.more-text {
  font-size: var(--font-sm);
  color: var(--text-muted);
}
.more-btn {
  height: 80rpx;
  padding: 0 56rpx;
  border-radius: 999rpx;
  background: var(--nb-page);
  color: var(--nb-text-link);
  font-size: var(--font-md);
  font-weight: var(--weight-semibold);
  display: flex;
  align-items: center;
  justify-content: center;
}
.more-btn:active { background: var(--nb-page); }
.content{padding:32rpx 32rpx calc(48rpx + env(safe-area-inset-bottom))}.block-head{padding:24rpx 8rpx 14rpx}.block-title{display:block;margin:0;font-size:var(--font-lg);font-weight:var(--weight-semibold)}.block-sub{display:block;margin-top:4rpx;font-size:var(--font-xs)}.guides,.faqs{padding:4rpx 28rpx;border-radius:24rpx;background:var(--nb-surface);box-shadow:none}.guide{min-height:104rpx;padding:22rpx 0;box-sizing:border-box;border-bottom:1rpx solid var(--nb-border)}.guide:last-child{border-bottom:0}.guide-num{width:44rpx;height:44rpx;background:var(--nb-action);font-size:var(--font-xs)}.guide-t{margin-bottom:5rpx;font-size:var(--font-sm);font-weight:var(--weight-medium)}.guide-d{font-size:var(--font-xs);line-height:1.6}.faq{min-height:96rpx;padding:26rpx 0;box-sizing:border-box;border-bottom:1rpx solid var(--nb-border)}.faq:last-child{border-bottom:0}.faq-q{font-size:var(--font-sm);font-weight:var(--weight-medium)}.faq-a-wrap{background:var(--nb-soft-surface)}.faq-a{font-size:var(--font-xs)}.more{padding-top:40rpx}.more-btn{min-height:88rpx;height:auto;border-radius:16rpx;background:var(--nb-surface);color:var(--nb-text-link);font-size:var(--font-sm)}
