.work-detail {
  min-height: 100vh;
  background: var(--nb-page);
  box-sizing: border-box;
}
.has-composer { padding-bottom: calc(176rpx + env(safe-area-inset-bottom)); }
.visitor-section {
  margin: 24rpx 0;
  padding: 24rpx;
  border-radius: 24rpx;
  background: var(--nb-page);
}
.visitor-artist {
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-height: 88rpx;
}
.visitor-avatar { width: 72rpx; height: 72rpx; border-radius: 50%; flex-shrink: 0; }
.visitor-copy { flex: 1; min-width: 0; }
.visitor-name { display: block; font-size: 28rpx; font-weight: 600; color: var(--nb-ink); }
.visitor-description { display: block; margin-top: 8rpx; font-size: 24rpx; line-height: 1.6; color: var(--nb-secondary); }
.visitor-link { font-size: 24rpx; color: var(--nb-text-link); flex-shrink: 0; }
.visitor-shop { margin-top: 20rpx; }
.info-panel.visitor-panel { padding-bottom: calc(150rpx + env(safe-area-inset-bottom)); }
.visitor-booking-bar { position: fixed; bottom: 0; left: 0; right: 0; z-index: 10; padding: 16rpx 28rpx calc(16rpx + env(safe-area-inset-bottom)); background: var(--nb-surface); }
.visitor-booking-button { min-height: 88rpx; border: 0; border-radius: 20rpx; background: var(--nb-text-link); color: var(--nb-surface); font-size: 28rpx; }
.visitor-booking-button::after, .visitor-share::after { border: 0; }
.visitor-booking-button:active { opacity: .8; }
.visitor-share { min-height: 88rpx; color: var(--nb-text-link); background: transparent; font-size: 26rpx; border: 0; }
.visitor-booking-note { display: block; margin-top: 24rpx; font-size: 24rpx; line-height: 1.6; color: var(--nb-secondary); }
.state-screen {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
}
.loading-spinner {
  width: 48rpx;
  height: 48rpx;
  border: 4rpx solid var(--nb-line);
  border-top-color: var(--nb-control);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
.error-screen { box-sizing: border-box; padding: calc(100px + env(safe-area-inset-top)) 32rpx calc(40rpx + env(safe-area-inset-bottom)); justify-content: flex-start; }
.error-card { box-sizing: border-box; width: 100%; max-width: 480px; padding: 40rpx 32rpx; border-radius: 24rpx; background: var(--nb-surface); }
.error-symbol { width: 52px; height: 52px; display: flex; align-items: center; justify-content: center; margin-bottom: 28rpx; border-radius: 16px; background: var(--nb-page); color: var(--nb-secondary); font-size: 28px; font-weight: 600; }
.state-title { display: block; font-size: 36rpx; line-height: 1.4; font-weight: 600; color: var(--nb-ink); }
.state-description { display: block; margin-top: 16rpx; font-size: 28rpx; line-height: 1.7; color: var(--nb-secondary); }
.error-actions { display: flex; gap: 20rpx; margin-top: 36rpx; }
.state-btn { box-sizing: border-box; display: flex; align-items: center; justify-content: center; min-width: 0; width: 100%; min-height: 44px; margin: 0; padding: 12px 10px; border-radius: 8px; border: 0; background: var(--nb-action); color: var(--nb-surface); font-size: 28rpx; line-height: 1.4; font-weight: 500; }
.error-actions .state-btn { flex: 1; }
.state-btn::after { border: 0; }
.state-btn.secondary { background: var(--nb-page); color: var(--nb-secondary); }
.state-pressed { opacity: 0.75; }
.error-support { margin-top: 36rpx; padding-top: 32rpx; border-top: 1px solid var(--nb-line); }
.support-title { display: block; color: var(--nb-ink); font-size: 28rpx; font-weight: 500; }
.support-description { display: block; margin: 12rpx 0 24rpx; color: var(--nb-muted); font-size: 24rpx; line-height: 1.7; }
.support-account { display: block; margin-bottom: 24rpx; color: var(--nb-secondary); font-size: 26rpx; overflow-wrap: anywhere; }
.top-action {
  position: fixed;
  z-index: 30;
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.52);
  box-sizing: border-box;
}
.top-back {
  left: 16px;
}
.top-back text {
  color: var(--nb-surface);
  font-size: 34px;
  line-height: 1;
  transform: translateY(-2px);
}
.image-swiper {
  width: 100%;
  height: 55vh;
  background: var(--nb-page);
}
.work-image {
  width: 100%;
  height: 100%;
}
.info-panel {
  position: relative;
  z-index: 2;
  margin-top: -36rpx;
  padding-bottom: calc(28rpx + env(safe-area-inset-bottom));
  background: var(--nb-surface);
  border-radius: 40rpx 40rpx 0 0;
  box-shadow: 0 -10rpx 30rpx rgba(0, 0, 0, 0.08);
  overflow: hidden;
}
.intro-section {
  padding: 38rpx 32rpx 28rpx;
}
.title-row {
  display: flex;
  align-items: center;
  gap: 20rpx;
}
.work-title {
  flex: 1;
  min-width: 0;
  color: var(--nb-ink);
  font-size: 38rpx;
  line-height: 1.35;
  font-weight: 700;
}
.manage-button {
  width: 88rpx;
  height: 88rpx;
  margin: -22rpx -16rpx -12rpx 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}
.manage-button:active {
  background: var(--nb-page);
}
.manage-button image {
  width: 36rpx;
  height: 36rpx;
}
.work-desc {
  display: block;
  margin-top: 14rpx;
  color: var(--nb-secondary);
  font-size: 27rpx;
  line-height: 1.65;
}
.tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 20rpx;
}
.tag {
  padding: 8rpx 18rpx;
  border-radius: 999rpx;
  background: var(--nb-page);
  color: var(--nb-ink);
  font-size: 22rpx;
}
.engagement-section {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8rpx;
  padding: 20rpx 24rpx;
  border-top: 0;
  border-bottom: 0;
  background: var(--nb-surface);
}
.engagement-action {
  min-width: 0;
  min-height: 44px;
  margin: 0;
  padding: 10rpx 4rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  border: 0;
  border-radius: 18rpx;
  background: var(--nb-page);
  color: var(--nb-secondary);
  font-size: 21rpx;
  line-height: 1.2;
  box-sizing: border-box;
}
.engagement-action::after {
  border: 0;
}
.engagement-action:active {
  background: var(--nb-page);
}
.engagement-action image {
  width: 32rpx;
  height: 32rpx;
}
.engagement-action.is-active {
  color: var(--nb-ink);
  background: var(--nb-page);
}
.action-count {
  font-size: 26rpx;
  color: var(--nb-secondary);
}
.title-row .booking-action {
  flex: 0 0 auto;
  min-height: 44px;
  margin: -6px 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
.booking-action-face {
  height: 32px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  border-radius: 6px;
  box-sizing: border-box;
  font-size: 13px;
  line-height: 1.2;
  white-space: nowrap;
  color: var(--nb-surface);
  background: var(--nb-action);
  font-weight: 600;
}
.booking-action:active .booking-action-face {
  opacity: 0.88;
}
.booking-action image {
  width: 16px;
  height: 16px;
  filter: brightness(0) invert(1);
}
.comments-section {
  padding: 30rpx 32rpx 34rpx;
}
.comments-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.comments-title {
  color: var(--nb-ink);
  font-size: 30rpx;
  font-weight: 700;
}
.comments-count {
  color: var(--nb-muted);
  font-size: 22rpx;
}
.comments {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
  margin-top: 26rpx;
}
.comment {
  display: flex;
  gap: 18rpx;
}
.avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  flex-shrink: 0;
}
.avatar.fallback {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--nb-page);
  color: var(--nb-ink);
}
.comment-body {
  flex: 1;
  min-width: 0;
}
.comment-name {
  font-size: 24rpx;
  font-weight: 600;
}
.badge {
  margin-left: 10rpx;
  color: var(--nb-ink);
  font-size: 20rpx;
}
.comment-time {
  margin-left: 12rpx;
  color: var(--nb-muted);
  font-size: 20rpx;
}
.comment-text {
  display: block;
  margin-top: 7rpx;
  color: var(--nb-secondary);
  font-size: 26rpx;
  line-height: 1.55;
}
.empty {
  margin-top: 22rpx;
  padding: 38rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  text-align: center;
  background: var(--nb-page);
  border-radius: 20rpx;
}
.empty-title {
  color: var(--nb-secondary);
  font-size: 25rpx;
  font-weight: 600;
}
.empty-sub {
  color: var(--nb-muted);
  font-size: 21rpx;
}
.hidden-toggle {
  min-height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--nb-ink);
}
.hidden-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  color: var(--nb-muted);
  font-size: 24rpx;
}
.composer-section {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 30;
  box-sizing: border-box;
  padding: 20rpx 24rpx calc(20rpx + env(safe-area-inset-bottom));
  border-top: 0;
  background: var(--nb-surface);
}
.replying {
  padding: 0 10rpx 12rpx;
  color: var(--nb-secondary);
  font-size: 22rpx;
}
.replying text {
  float: right;
  color: var(--nb-ink);
}
.input-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}
.comment-input {
  flex: 1;
  min-width: 0;
  height: 44px;
  padding: 0 26rpx;
  border-radius: 8px;
  background: var(--nb-page);
  box-sizing: border-box;
  font-size: 25rpx;
}
.send {
  flex: 0 0 72px;
  width: 72px;
  min-width: 72px;
  max-width: 72px;
  height: 44px;
  margin: 0;
  padding: 0;
  flex-shrink: 0;
  display: block;
  text-align: center;
  border-radius: 8px;
  background: var(--nb-action);
  color: var(--nb-inverse);
  font-size: 14px;
  font-weight: 600;
  line-height: 44px;
  box-sizing: border-box;
}
.send::after { border: 0; }
.send-pressed { opacity: 0.85; }
.work-price-row {
  margin-top: 12rpx;
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
  gap: 10rpx 16rpx;
}
.work-promotion {
  display: flex;
  align-items: baseline;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 18rpx;
  padding: 16rpx 18rpx;
  border-radius: 12rpx;
  background: var(--nb-page);
}
.work-promotion-title { color: var(--nb-ink); font-size: 26rpx; font-weight: 600; }
.work-promotion-amount { color: var(--nb-money); font-size: 26rpx; font-weight: 600; }
.work-promotion-note { width: 100%; color: var(--nb-secondary); font-size: 22rpx; }
.work-price { color: var(--nb-money); font-size: 38rpx; font-weight: 700; font-variant-numeric: tabular-nums; }
.work-price-note { color: var(--nb-muted); font-size: 22rpx; }
.work-price-note.discount { color: var(--nb-money); }
.work-price-note.surcharge { color: var(--nb-secondary); }
.comment-meta { display: flex; align-items: center; justify-content: space-between; gap: 12rpx; }
.comment-meta-copy { min-width: 0; display: flex; align-items: center; flex-wrap: wrap; }
.badge.pinned { color: var(--nb-ink); background: var(--nb-page); }
.comment-manage { width: 88rpx; min-height: 88rpx; margin: -26rpx -16rpx -26rpx 0; display: flex; align-items: center; justify-content: center; color: var(--nb-muted); font-size: 24rpx; letter-spacing: 2rpx; }
.hidden-comment { display: flex; align-items: center; justify-content: space-between; gap: 16rpx; }
.hidden-comment-copy { flex: 1; min-width: 0; }

@import '../../styles/semantic-actions.wxss';
@import '../../styles/booking-actions.wxss';

.visitor-booking-button.booking-action { min-height:44px; height:44px; border-radius:8px; font-size:var(--font-sm); line-height:1.4; box-shadow:none; }
