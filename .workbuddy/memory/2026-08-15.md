# 2026-08-15

## 客户端小程序作品详情页改版（client-wxapp/pages/client/work-detail/）
- 修复底栏异常：原布局把输入框 + 点赞 + 收藏 + 评论 + 分享 + 预约同款全挤一行，`flex:1` 输入框被压没。
- 底栏改为小红书式：输入框在左，点赞/收藏/评论/分享图标跟在输入框后方（图标+数字竖排，min-width 76rpx）；有输入文字时在输入框右侧显示「发送」。
- 【预约同款】按钮从底栏移至作品图下方内容面板标题行右侧（title-row + title-book-btn，右对齐，88rpx 触控高度，品牌渐变）。
- 顺带修复：评论图标跳转评论区原来用 wx.pageScrollTo（在 scroll-view 内无效），改为 scroll-view 的 scroll-into-view 定位 #work-comments。

## 客户端小程序作品详情页统一设计（登录态 vs 未登录态）
- 用户要求：登录态 work-detail 与未登录态 public-work 保持完全一致，以 public-work 为标准。
- 重构 work-detail 全部三个文件（wxml/wxss/js）+ json，完全对齐 public-work 的布局和视觉：
  - 导航栏：自定义玻璃胶囊 → `<nav-bar>` 标准组件（json 加 usingComponents）
  - 布局：scroll-view + 固定底栏 → container > content 内自然滚动（去掉 disableScroll）
  - 信息面板：title-block + aesthetic-meta + comments → title-section + tags-section + tech-section + actions-section + comments-section（与 public-work 一致）
  - 操作按钮：底栏竖排图标 → 内容区内横排「喜欢/收藏/分享」按钮（CSS icon + 高亮态）
  - 底栏：评论输入 + 图标组 → 评论输入 + 预约同款（与 public-work 一致）
  - 图片查看：自定义全屏 viewer → wx.previewImage（与 public-work 一致）
- 保留登录态特有功能：实际点赞/收藏 toggle、评论提交、回复评论、分享路径准备
