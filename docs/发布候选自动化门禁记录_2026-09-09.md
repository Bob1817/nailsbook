# 发布候选自动化门禁记录

> 候选提交：`fa534d95`  
> 验证环境：detached 干净工作树 `/tmp/nailbook-rc-d7059272-verify`  
> 验证日期：2026-09-09

## 结论

本地干净候选的后端、超管后台、官网、小程序和颜色门禁全部通过。首次干净运行发现后端 Jest 依赖未跟踪 `.env`，已补充公开的测试专用密钥初始化并在无 `backend/.env` 的干净工作树复验通过。

当前提交尚未推送，因此没有与 `fa534d95` 对应的远端 CI 结果；RC 标签需等待当前提交的远端 CI 全绿后创建。

## 结果

| 模块 | 命令 | 结果 |
| --- | --- | --- |
| 后端 | `npm test -- --runInBand` | 98 suites、462 tests、2 snapshots 全部通过 |
| 后端 | `npm run build` | 通过 |
| 超管后台 | `npm run lint && npm run build` | 通过 |
| 官网 | `npm run test:run && npm run build` | 1 file、4 tests 通过，构建通过 |
| 微信小程序 | `node scripts/release-gate.js` | 1 项全量静态检查、76 项专项检查通过 |
| 颜色 | `python3 scripts/sync-colors.py --check && python3 scripts/check-colors.py` | 通过 |

## 构建目录校验值

- `backend/dist`：`efa8669d974d6804dacb8db35448e770e2dd760cfcb22513d7a2318c2693388e`
- `admin-frontend/dist`：`c80de74830c81a35bc221d4d0c8b0dae434536f3ac9fcb94db49b41902fa0d64`
- `website/dist`：`f004afabdd9dadc65f0f48717ef1c8bce6c414e0454e2d4ad0ae08ac4b9963cb`

以上校验值用于标识本次本机构建目录；正式发布仍以 CI 生成的 commit SHA 镜像 digest 为准。

## 发布范围说明

- 本次发布：微信小程序、后端 API、超管后台。
- 官网执行构建兼容检查，但不作为本轮核心业务入口。
- Client Web、Technician Web、Flutter 与 iOS 不随本次发布，不产生发布产物。

## 待完成

- 推送当前分支并等待 `fa534d95` 及后续文档提交对应 CI 全绿。
- 记录 CI 链接、镜像 digest 和最终候选 SHA。
- CI 全绿且 P0 外部阻塞关闭后创建 RC 标签。
