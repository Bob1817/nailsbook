# 生产安全配置验收记录

> 核查日期：2026-09-09  
> 范围：代码仓库、本机发布配置、生产公开接口

## 当前结论

代码侧未发现真实 JWT、微信密钥或私钥被 Git 跟踪；三类 JWT 密钥的本机发布配置均已配置、长度不少于 32 位且未命中默认弱值。本轮还移除了超管创建美甲师时固定使用 `123456` 的逻辑，改为每个账号生成独立 12 位随机临时密码，并继续要求首次登录改密。

生产侧仍需要服务器负责人登录宿主机复核密钥实际来源、文件权限、备份目录权限和密钥轮换记录。本机 `.env` 文件权限已收紧为仅当前用户读写，但该结果不能代替 ECS 核查。

## 已完成

- [x] Git 跟踪文件敏感模式扫描；唯一私钥命中为管理后台输入框占位符，不含真实私钥。
- [x] `.env`、`.env.production` 和管理后台本机环境文件均未被 Git 跟踪。
- [x] 本机敏感环境文件权限收紧为 `0600`。
- [x] Admin、Technician、Client JWT 本机配置长度与默认弱值检查通过；扫描过程未输出密钥内容。
- [x] 生产 API 对未登录管理端请求返回 `401`。
- [x] 生产 API 不向未授权来源返回 CORS 允许头。
- [x] 后端已配置受限可信代理、全局限流以及登录、验证码、上传和公开接口专项限流。
- [x] 图片与音频上传配置包含类型和大小限制。
- [x] 新建美甲师使用独立随机临时密码；专项测试 4 项和后端构建通过。
- [x] Nginx 配置增加 HSTS、`nosniff`、同源嵌入和 Referrer Policy 基线；部署后需再从公网复验响应头。

## 发布前外部复核

- [ ] 在 ECS 确认三类 JWT 密钥和系统配置加密密钥互不相同、长度合规，并记录轮换日期。
- [ ] 确认生产 `.env`、数据库、上传目录和备份目录仅发布用户/容器所需账号可读。
- [ ] 部署新 Nginx 配置后运行 `nginx -t`，并从公网核对四项安全响应头。
- [ ] 使用三个角色测试令牌复验跨角色请求均返回 `401/403`。
- [ ] 复验禁用超管立即失效，敏感管理操作可在审计日志查询。
- [ ] 轮换任何曾通过聊天、截图或临时文件传递过的生产密码或密钥。

## 风险判断

当前未发现仓库内高危密钥泄露。服务器权限、实际密钥轮换和已部署响应头尚无主机侧证据，P0-06 暂不能标记整体完成。
