# 作品分享后端发布记录 · 2026-08-28

## 发布结果

- 已按用户授权通过 SSH 推送并部署至现有线上 ECS `47.96.159.106`，不是新增测试环境。
- 分支：`codex/backend-staged-release-202608`。
- 后端代码提交：`f76c3fd2656fdd80f575cc178c79fd5c8187a91a`，31个文件；未包含小程序、H5、客户状态卡片或收入日历等其他未提交改动。
- GitHub SSH 22连接失败，改用443及已有 github.com 主机密钥校验完成推送；未关闭主机校验。
- 发布时间：2026-08-28 19:37:04（Asia/Shanghai）。
- 发布目录：`/opt/nailbook-releases/f76c3fd2656fdd80f575cc178c79fd5c8187a91a`。
- 镜像：`nailbook-backend:sha-f76c3fd2656fdd80f575cc178c79fd5c8187a91a`。
- 实际镜像ID：`sha256:b01db77254eb96d676f1cc89ce4c1e8d12024d98787adb15356c5c3e0d8a1d07`，linux/amd64。

## 构建与验证

- 从 git archive 的已提交快照执行本地构建，不使用未提交后端源码。全量测试最初缺少跨目录小程序契约文件，补齐同提交的 client-wxapp 快照后重新运行：90个套件、382项测试、2个快照全部通过；npm run build通过。
- 本次不是 GitHub Actions 新构建。线上与候选 package-lock.json、Prisma schema SHA-256逐一相同，依赖及迁移无改动；复用上一版已验证的不可变运行镜像，仅清除旧 dist 并复制候选编译产物生成新镜像。通过SSH传输产物及源码快照，服务器未运行 npm install 或源码编译。
- 发布前补充归档作品不得用于分享绑定的过滤条件及回归测试。
- SQLite在线一致性备份保存在发布目录 backup/prod.db；完整性及外键检查通过，校验和保存在 backup/checksum.txt。
- Compose仅替换backend镜像，保留数据库卷、上传目录、环境配置和127.0.0.1端口限制；其他五个服务容器ID及镜像均未改变。Nginx仅验证配置并重载以刷新后端地址。
- 启动日志：无待执行迁移，应用启动成功；后端running，重启0次，OOM=false。

## 上线只读检查

| 检查 | 结果 |
| --- | --- |
| HTTPS /api/health | 200，连续3次检查通过 |
| /api/public/works | 200，本次返回空列表，未验证有效作品详情数据 |
| 新分享码路由传入无效令牌 | 400，路由存在且参数校验生效；未调用真实微信出码 |
| 未登录 POST /api/client/auth/bind-shared-work | 401，无业务写入 |
| 未登录客户/美甲师订单 | 401 |
| /api/docs | 404 |

激活脚本输出 ACTIVATION_SUCCEEDED；未触发回滚。证据文件为 activation.log、tests.log、health.json、containers-before.txt、containers-after.txt。

## 运维与回滚

后续维护使用新发布目录的Compose配置，不能直接执行旧 deploy/push-deploy.sh（会git add -A且使用旧配置）。

```bash
release=/opt/nailbook-releases/f76c3fd2656fdd80f575cc178c79fd5c8187a91a
docker compose --project-directory /opt/nailbook -p nailbook \
  -f "$release/docker-compose.yml" -f "$release/ecs.override.yml" ps
```

仅回滚后端应用，保留数据库最新写入：

```bash
old=/opt/nailbook-releases/a2e499512bd088135ce973de5ae2392f3814d554
docker compose --project-directory /opt/nailbook -p nailbook \
  -f "$old/docker-compose.yml" -f "$old/ecs.override.yml" \
  up -d --no-deps --no-build --pull never backend
docker exec nailbook-nginx-1 nginx -s reload
curl --fail https://api.lunails.cn/api/health
```

没有执行小程序上传、发布、真实注册、绑定、预约、短信或支付。真实微信出码/相册/扫码和客户完整预约链路仍需专用测试账号验收；本次上线检查不能代替该验收。完整跨渠道归因也不在本次发布完成声明内。
